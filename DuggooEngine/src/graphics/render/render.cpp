#pragma once

#include "renderer.h"


namespace dg { namespace graphics {

	
	void Renderer::render(VertexArray& va, IndexBuffer& ib, Shader& shader)
	{
		// vertex array
		//va.bind();
		// index buffer
		// shader
	}

} }